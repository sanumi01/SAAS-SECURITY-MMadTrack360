# Example env for dev server SES integration
# To enable real SES sends, set these values. If omitted the server uses a safe console stub.
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_REGION=us-east-1
SES_SENDER=no-reply@yourdomain.com
# Optional: application public URL used in reset links
APP_URL=http://localhost:4173
PORT=4000
